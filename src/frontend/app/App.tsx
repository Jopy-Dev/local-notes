import { LaunchPage } from "../pages/LaunchPage";

// AppRouter (MasterPrompt.md 1.6) lands at Step 12; launch surface is the
// only routed screen during the Step 11 design port.
export function App() {
  return <LaunchPage />;
}
